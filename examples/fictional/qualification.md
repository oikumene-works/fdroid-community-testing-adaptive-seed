# Fictional Built-APK Qualification

**PASS FOR RECORD-SHAPE DEMONSTRATION ONLY — NO APK EXISTS**

The example shows an expected package `org.example.imagemop`, version `1.0.0`
(`1`), minimum SDK 29, target SDK 34, no permissions, and the synthetic APK and
certificate digests in `case.env`. Features and native code are `NONE`; the
manifest-tree digest is synthetic. A real helper also reports components and
relevant manifest lines before deleting the APK.

No download, parser, certificate, alignment, emulator, ADB, installation, or
upload action occurred. A real qualification may be marked `PASS` only after
the separately approved exact APK is inspected and deleted at a clean
checkpoint.
