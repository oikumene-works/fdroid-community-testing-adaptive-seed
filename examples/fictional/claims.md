# Fictional Public Claim Review

**SYNTHETIC EXAMPLE**

TEST_SAFETY_STATUS=PASS
CLAIM_REVIEW_STATUS=PASS

The fictional scope uses synthetic images only, no account or network, and
disposable storage. These decisions are not approval for any real action.

## Exact sources

The invented listing says the app creates cleaned copies and never changes an
input. The imagined README and localized description are pinned only by the
synthetic values in `case.env`.

## Claim-to-behavior matrix

| Claim | Fictional evidence | Assessment |
| --- | --- | --- |
| Inputs are never changed. | The imagined source opens inputs read-only and creates outputs through a document-create path. | `consistent` |
| No network access. | The imagined source and merged manifest contain no `INTERNET` permission. | `consistent` |
| PNG and JPEG copies are supported. | Both invented format branches appear in the imagined source checklist. | `consistent` statically; runtime is not real. |

## Gate decision

`PASS` for demonstration only. `EXAMPLE_ONLY=true` overrides this value and
prevents activation, download, emulator use, ADB use, installation, or posting.
