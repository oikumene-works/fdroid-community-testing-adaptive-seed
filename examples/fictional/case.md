# Fictional Image Mop MR !90001

**SYNTHETIC EXAMPLE — NO REAL PROJECT OR EXECUTABLE**

## Gate state

- [x] Fictional selection demonstrated
- [x] Fictional exact source and claim preflight demonstrated
- [x] Fictional qualification record demonstrated
- [ ] Execution not authorized or performed
- [ ] Report not a real candidate report
- [ ] Posting permanently disabled

## Exact candidate

The imagined case pins MR head `1111111111111111111111111111111111111111`,
pipeline `90000003`, build job `90000004`, source
`2222222222222222222222222222222222222222`, version `1.0.0` (`1`), and the
synthetic APK digest recorded in `case.env`.

## Intended bounded test

The imagined app copies a synthetic PNG and JPEG into a selected output folder
while removing an invented `Comment` metadata field. A test would verify input
hashes remain unchanged, output metadata is absent, decoded pixels match, a
cancelled picker leaves no output, and no permission or network surface exists.

Only the visible, wiped project-local API 34 AVD would be used. Execution would
stop on any changed pin, permission, account/network surface, target ambiguity,
unexpected input mutation, crash/ANR, or cleanup failure.

## Execution and cleanup

No APK was downloaded, no emulator or ADB server was started, and no external
mutation occurred. This example can never be activated because
`EXAMPLE_ONLY=true`.
